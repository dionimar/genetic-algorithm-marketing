Las carpetas se corresponden con 10 instancias distintas de cada tamaño del problema con el fin de analizar si existen diferencias entre las configuraciones para cada conjunto de 10 instancias por tamaño.
Los archivos sueltos representan los datos de una instancia particular del problema con tamaño dado por el nombre, donde se analizan la mejor, media y heurística de cada caso.

